import 'package:flutter/material.dart';

class ColorConstants {
  static Color? dark = Colors.blue[900];
  static Color? light = Colors.blue[50];
  static Color? emergency = Colors.red[500];
  static Color? selected = Colors.blue[500];
  static Color? searched = Colors.cyan[200];
}
